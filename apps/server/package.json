{
  "name": "@cc-map/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "install-hook": "node scripts/setup-hook.mjs"
  },
  "dependencies": {
    "@cc-map/parser": "*",
    "fastify": "^5.2.1"
  }
}
